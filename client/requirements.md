## Packages
framer-motion | Page transitions and animations

## Notes
API endpoint /api/generate-card expects multipart/form-data
Response is a binary blob (ZIP or HTML file) that needs to be downloaded
