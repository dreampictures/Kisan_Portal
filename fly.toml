# fly.toml - Fly.io configuration
# ਪਹਿਲੀ ਵਾਰ: fly apps create kisan-union-punjab
# ਫਿਰ: fly deploy

app = "kisan-union-punjab"
primary_region = "bom"

[build]

[env]
  NODE_ENV = "production"
  PORT = "8080"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

  [[http_service.checks]]
    grace_period = "15s"
    interval = "30s"
    method = "GET"
    path = "/"
    timeout = "10s"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
